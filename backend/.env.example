# Required for AI report generation (metrics work without it)
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
REPORT_MODEL=claude-fable-5
REPORT_MAX_TOKENS=8000
MAX_UPLOAD_BYTES=26214400

# Comma-separated list; set to your Streamlit app URL in production
ALLOWED_ORIGINS=*
